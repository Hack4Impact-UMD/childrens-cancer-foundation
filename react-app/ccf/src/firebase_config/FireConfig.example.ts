// Template for src/firebase_config/FireConfig.ts (which is gitignored).
// Copy this file to FireConfig.ts and fill in the values from your Firebase
// project settings (Project settings > General > Your apps > SDK setup).
// These web-app config values are client-side identifiers, not secrets.
const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};

export default firebaseConfig;
